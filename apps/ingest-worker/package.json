{
  "name": "ingest-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --persist-to ../../.wrangler/state",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "test": "wrangler deploy --dry-run --outdir .test-build && node --test test/ingest.test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@sight-cache/worker-home-page": "*",
    "hono": "^4.13.7",
    "typescript": "^7.0.2"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
